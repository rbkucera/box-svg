// ===========================
// Mailer box proportions
// ===========================

/** Tuck flap depth as fraction of box height */
export const MAILER_TUCK_DEPTH_RATIO = 0.5;

/** Tuck flap taper (each side) as fraction of box height */
export const MAILER_TUCK_TAPER_RATIO = 1 / 8;

/** Side flap width as fraction of opposing dimension */
export const MAILER_FLAP_WIDTH_RATIO = 0.5;

/** Lid fit thickness adjustment multiplier (applied to T) */
export const MAILER_LID_THICKNESS_RATIO = 0.5;

/** Bevel inset equals material thickness (45-degree angle) */
export const MAILER_BEVEL_INSET_MULTIPLIER = 1.0;

// ===========================
// Tuck-top box proportions
// ===========================

/** Dust flap depth as fraction of box width */
export const TUCKTOP_DUST_DEPTH_RATIO = 0.5;

/** Tuck flap depth as fraction of box width */
export const TUCKTOP_TUCK_DEPTH_RATIO = 1.0;

/** Closure panel depth as fraction of box width */
export const TUCKTOP_CLOSURE_DEPTH_RATIO = 0.5;

/** Glue bevel height limit as fraction of box height */
export const TUCKTOP_GLUE_BEVEL_HEIGHT_RATIO = 1 / 6;

/** Glue bevel width limit as fraction of glue tab width */
export const TUCKTOP_GLUE_BEVEL_WIDTH_RATIO = 0.5;

/** Tuck inset limit as fraction of box length */
export const TUCKTOP_TUCK_INSET_LENGTH_RATIO = 1 / 8;

/** Tuck inset limit as fraction of tuck depth */
export const TUCKTOP_TUCK_INSET_DEPTH_RATIO = 1 / 3;

/** Glue tab min width as fraction of box width */
export const TUCKTOP_GLUE_TAB_MIN_RATIO = 0.25;

/** Glue tab max width as fraction of box width */
export const TUCKTOP_GLUE_TAB_MAX_RATIO = 0.5;

/** Glue tab min width as multiplier of thickness */
export const TUCKTOP_GLUE_TAB_THICKNESS_MULTIPLIER = 4;

/** Dust flap taper min as fraction of box width */
export const TUCKTOP_DUST_TAPER_MIN_RATIO = 1 / 8;

/** Dust flap taper max as fraction of box width */
export const TUCKTOP_DUST_TAPER_MAX_RATIO = 0.25;

/** Dust flap taper min as multiplier of thickness */
export const TUCKTOP_DUST_TAPER_THICKNESS_MULTIPLIER = 2;
